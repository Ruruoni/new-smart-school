// A student's own report card. Same page as the parent portal: the API scopes a student to themself
// (/me/children returns the student as "Self"), and the financial lockout applies to them exactly as to a parent.
export { default } from "../../portal/results/page";
