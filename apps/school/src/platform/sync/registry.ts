// The registry lives in the shared protocol package so the cloud validates exactly what the school sends.
export { SYNC_ENTITIES, isSyncEntity, unexpectedFields, type SyncEntityType, type ConflictPolicy, type SyncEntityDef } from "@smartschool/protocol";
